# 2026 정부혁신박람회 디자인 시스템

## 디자인 명제

**제도적 신뢰를 바탕으로, 국민의 변화를 편집물처럼 선명하게 보여준다.**

행정 안내 사이트의 확실성과 접근성을 유지하되, 박람회 홍보물처럼 기억되는 리듬을 만든다. 장식보다 정보의 우선순위, 타이포그래피, 면과 선의 대비가 브랜드를 만든다.

## 참고 체계와 적용 원칙

- **StyleSeed** — `institutional-service` 문법을 기반으로 `expressive-marketing`의 추진력을 제한적으로 결합한다. 화면 순서는 제안 → 판단 기준 → 구체 사례 → 과거 근거 → 일정 → 행동 유도로 고정한다.
- **Impeccable** — 반복되는 동일 카드, 과도한 라운드, 그림자, 그라데이션, 평평한 타이포 계층을 배제한다. 최종 검수는 위계·반응형·접근성·모션 비용 순으로 수행한다.
- **IBM × WIRED** — IBM의 단단한 블루·평면·헤어라인 체계 위에 WIRED의 비대칭 편집 그리드와 큰 제목, 섹션 번호를 얹는다.
- **Design Farmer** — 색상은 primitive → semantic → component 순으로 사용한다. 실제 구현의 기준은 `tokens.css`이며 임의의 색·간격을 새로 만들지 않는다.
- **Amicro** — 하나의 Silk 모션 언어만 쓴다. 스크롤 진행선, 160–220ms 상호작용, 560ms 진입 모션을 사용하고 콘텐츠 표시는 지연하지 않는다.
- **UI UX Pro Max** — `Event/Conference Landing`과 `Accessible & Ethical` 추천을 채택한다. 수요조사 CTA는 스크롤 뒤에도 유지하고, 모바일 터치 대상은 44×44px 이상, 날짜·D-day 숫자는 tabular figures로 표시한다.

## 색상 역할

- `surface-brand`: 행사 소개와 신뢰를 담당하는 잉크 남색
- `brand-primary`: 링크, 정보 강조, 현재 위치
- `brand-secondary`: 박람회의 변화와 연결을 나타내는 보조 신호
- `action-primary`: 수요조사·문의 등 실제 행동에만 사용하는 라임
- `surface-page` / `surface-subtle`: 콘텐츠 호흡을 만드는 백색과 옅은 회청색

한 화면에서 경쟁하는 강조색을 만들지 않는다. 라임은 행동과 진행 상태에만, 남색과 맑은 청색은 구조와 탐색에만 사용한다. 청록은 사용하지 않는다.

## 타이포그래피

- 제목: 큰 크기, 촘촘한 자간, 짧은 행. 의미 단위로 줄을 나눈다.
- 본문: 1.6 전후 행간, 최대 폭 42rem.
- 섹션 번호·상태: 모노 계열 숫자를 사용해 아카이브와 편집물의 인상을 만든다.
- 본문에서 굵기는 400/600/700만 사용한다.

## 공간과 형태

- 주요 섹션 간격: `--space-section`
- 카드보다 선과 면의 분할을 우선한다.
- 모서리는 0–3px, 그림자는 사용하지 않는다.
- 동등하지 않은 콘텐츠를 3등분하지 않는다. 대표 사례는 크게, 보조 사례는 짧고 밀도 있게 배치한다.

## 상호작용

- 버튼: hover에서 최대 2px 상승, active에서 `scale(.97)`
- 링크 화살표: 4px 이내 이동
- 진입: `opacity + translateY`, 최대 560ms
- 스크롤 진행: 상단 3px 선으로 현재 위치만 표시
- 헤더: 첫 화면에서는 행사 포스터의 일부로 보이고, 스크롤 뒤에는 64px 안내 바로 축약해 참여 기준 CTA를 유지
- 모바일 메뉴: 72% 차광막으로 본문과 탐색을 분리하고, 열린 동안 본문 스크롤을 잠근다. `Esc`로 닫으면 메뉴 버튼에 포커스를 돌려준다.
- `prefers-reduced-motion`에서는 진입과 이동 효과를 제거한다.

## 접근성 기준

- 키보드 포커스는 황색 3px 외곽선으로 모든 배경에서 식별 가능해야 한다.
- 모든 터치 대상은 최소 44px를 확보한다.
- 색상만으로 상태를 전달하지 않는다.
- 장식 요소는 접근성 트리에서 제외한다.
- 모바일에서는 콘텐츠 우선순위를 유지하며 비대칭 그리드를 단일 열로 재배치한다.
- 460px 이하에서는 섹션 설명과 보조 링크를 제목 본문의 42px 편집 축에 맞춘다.

## KRDS 정렬 (2026-08-01)

KRDS(대한민국 정부 디자인시스템, v04.krds.go.kr) 핵심 요소를 기존 편집 디자인 위에 정렬했다. 전면 전환이 아니라 스타일 기준 정렬이다.

- **서체**: Pretendard GOV (KRDS 공식 서체). KRDS-uiux/krds-uiux 저장소의 공식 서브셋 woff2(Regular 400·Medium 500·Bold 700)를 `fonts/`에 셀프호스팅 — 외부 CDN 호출 없음.
- **색상**: 파랑·네이비 primitive를 KRDS light primary 토큰에 정렬 — navy-950→primary-90(#03163a), navy-900→primary-80(#052561), blue-700→primary-60(#0b50d0), blue-100→primary-5(#ecf2fe), sky-300→primary-30(#86aff9). 라임 액션 컬러와 슬레이트 텍스트는 사이트 고유 아이덴티티로 유지. oklch 재정의 블록에서 파랑 계열은 제거(정확한 KRDS hex 유지 목적).
- **공식 배너 제외**: KRDS 공식 배너(태극기+공식 누리집 표기)는 .go.kr 공식 누리집 전용이므로 github.io 페이지에는 적용하지 않고, 마스트헤드 문구도 안내 페이지임을 밝히는 문구로 조정.

### 액센트 변경 (2026-08-01)

라임 액션 컬러를 KRDS Primary-30(#86aff9, hover Primary-20 #b1cefb)으로 교체해 남색·파랑 모노톤으로 통일. 사례 카드 심볼의 라임 계열과 파비콘 막대도 함께 정렬. `--lime` 별칭은 하위 호환용으로 유지하되 이제 하늘빛을 가리킨다.

### 안티슬롭 정리 (2026-08-01, killaislop.com 기준)

35개 슬롭 패턴 감사 결과 4건 제거: ① 섹션 번호 칩(01~06)과 그 그리드 컬럼, ② 전 카드 동일했던 "공식 출처 확인" 뱃지(섹션 문구+출처 링크가 신호 담당), ③ 사례 카드의 "01 /" 번호 접두사, ④ 콜아웃의 원형 i 아이콘. 위계는 크기·여백으로만 표현한다. 통과 항목: 단일 강조색, 실측 수치만 사용, 단일 서체, 이모지·그라데이션 없음.

### 스캐너 기반 2차 정리 (2026-08-01, yetone/kill-ai-slop scan.mjs)

공식 스캐너 실행 결과 26건 중 확인된 실제 슬롭 3건 제거: ① 죽은 CSS(.eyebrow/.section-kicker — HTML 미사용), ② 히어로 세로 영문 폴리오("GOV INNOVATION EXPO", mono 장식), ③ 이에 따라 불필요해진 --font-mono 토큰. 잔여 20건은 검토 후 오탐으로 판정: 링크 밑줄(이메일), 대형 디스플레이 타이포(의도된 clamp), 시맨틱 화살표 ↗→↓(외부링크·동작·스크롤 표시), 원형 점(타임라인·심볼). 재스캔 시 이 판정 유지.

### 아카이브 현장 사진 (2026-08-01)

연도별 카드에 행안부 사진뉴스 게시판(type002)의 현장 사진을 1장씩 반영 (`photos/expo-YYYY.jpg`, 640px·75품질 최적화, 원본 EXIF로 행사일 일치 확인). 카드마다 원 게시물 링크로 출처 표시. 2020년은 온라인 개최라 개최 브리핑 사진이며 캡션에 명시.

라이선스 참고: 행안부 보도자료 게시판(type010)은 공공누리 1유형(출처표시) 마크가 있으나, 사진이 첨부된 사진뉴스 게시판(type002)에는 게시물 단위 마크가 없어 엄밀히는 미표시 상태. 행안부 저작물이므로 출처표시 방식으로 게재하되, 필요시 혁신기획과에서 자체 확인 가능. 이미지 직접 URL 패턴: mois.go.kr/cmm/fms/getImage.do?atchFileId=...&fileSn=N
